{% extends 'base.html.twig' %}

{% block title %}Panneau Admin{% endblock %}

{% block body %}
    <div class="max-w-4xl mx-auto p-6">

        <h1 class="text-3xl font-bold text-center mb-12 text-gray-800">Panneau d'administration</h1>

        <nav class="grid grid-cols-1 md:grid-cols-2 gap-10">
            <a href="{{ path('app_screening') }}"
               class="block py-12 px-8 rounded-lg bg-indigo-600 text-white font-semibold text-xl text-center hover:bg-indigo-700 transition">
                Séances
            </a>
            <a href="{{ path('app_film') }}"
               class="block py-12 px-8 rounded-lg bg-indigo-600 text-white font-semibold text-xl text-center hover:bg-indigo-700 transition">
                Films
            </a>
            <a href="{{ path('app_schedule') }}"
               class="block py-12 px-8 rounded-lg bg-indigo-600 text-white font-semibold text-xl text-center hover:bg-indigo-700 transition">
                Horaires
            </a>
            <a href="{{ path('app_rooms') }}"
               class="block py-12 px-8 rounded-lg bg-indigo-600 text-white font-semibold text-xl text-center hover:bg-indigo-700 transition">
                Salles
            </a>
        </nav>

    </div>
{% endblock %}
